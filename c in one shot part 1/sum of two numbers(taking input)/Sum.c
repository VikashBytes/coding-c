#include <stdio.h>
int main(){
    int x, y, z;
    printf("Enter the value of X :");
    scanf("%d",&x);
    printf("enter the value of y :");
    scanf("%d",&y);
    z = x + y;
    printf("sum : %d",z);
    return 0;
}